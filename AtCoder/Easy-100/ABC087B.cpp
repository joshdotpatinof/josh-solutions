// written by josh.patinof
#include <bits/stdc++.h>
using namespace std;

using ll = long long;
#define fast_io ios_base::sync_with_stdio(false); cin.tie(NULL);

void solve() {
    int a, b, c; cin >> a >> b >> c;
    int x; cin >> x;
    vector<int> dp(x + 1);
    for (int i = 1; i <= x; ++i){
        
    }

    cout << dp[x] << endl;
}

int main() {
    fast_io;
    //int t = 1;
    //cin >> t;
    //while (t--) {
    solve();
    //}
    return 0;
}